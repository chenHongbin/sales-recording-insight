---
name: sales-recording-insight
description: >
  电销录音管理教练——面向电话销售管理者，批量分析团队录音并生成管理洞察。
  自动本地转写、6维话术评分、关键标签、改进建议、团队横向对比和趋势诊断。
  核心价值：把"听录音"从1小时变成5分钟，把"管团队"从靠感觉变成靠数据。
  使用场景：
  (1) 分析单通销售录音——输入录音文件，生成话术评分和改进建议
  (2) 批量分析团队录音——输入多通/文件夹/日期范围，生成团队诊断和横向对比
  (3) 周度/月度团队复盘——分析一段时间内的全部录音，输出趋势和训练主题
  (4) 每日管理辅助——早上看高意向和异常、中午做纠偏、晚上做复盘
  触发词：分析录音、录音洞察、销售复盘、团队录音分析、复盘录音、批量分析录音、这周录音分析、团队通话质量。
  不适用：非销售场景录音、仅快速转写无分析需求、视频文件。
---

# 电销录音管理教练

> 把销售对话从"听感经验"变成"可分析、可复盘、可训练的数据资产"。
> 管理者视角，批量分析，团队诊断，数据驱动。

## 核心设计

**默认假设：用户是销售管理者，通常要分析多通录音。**
单通分析是子流程，批量团队分析是核心能力。

**分析后必须回答的管理问题：**
1. 这通/这批电话里，客户说了什么？销售做了什么？
2. 谁的通话质量好，谁的问题最多？
3. 团队共性短板是什么？本周训练主题是什么？
4. 哪些客户值得重点跟进？哪些被浪费了？
5. 明天一对一辅导谁、说什么？

**核心承诺：**
- 录音数据不外传——只用本地 Whisper
- 每份报告聚焦"管理动作"——不仅分析电话，更告诉你怎么管人
- 批量分析必出团队诊断——只给单通报告等于没做批量分析
- 通用不绑定产品——任何行业的电话销售都适用

---

## 工作流程

### 批量分析流程（默认）

```
用户提供录音（多通/文件夹/日期范围）
    │
    ▼
阶段 0：汇总录音清单
    - 扫描文件夹，收集所有录音文件
    - 按销售姓名分组
    - 确认分析范围（多少通、哪些人、什么时间段）
    │
    ▼
阶段 1：逐通转写
    - 每通独立转写为 /tmp/recording_N.txt
    - 汇总转写统计（总时长、总字数）
    │
    ▼
阶段 2：逐通分析（6大维度）
    - 每通按同一框架分析，生成独立 JSON
    │
    ▼
阶段 3：团队汇总诊断（批量核心）
    - 横向对比：谁强谁弱、进步退步
    - 标杆萃取：>=8分录音的成功模式
    - 短板归因：<=4分录音的问题地图
    - 异议情报：客户异议分布、趋势、解决建议
    - 产品审计：客户理解偏差、话术优化建议
    - SOP审计：执行率、卡点、优化建议
    - 三层清单：优秀/问题/回炉
    - 培训处方：必修/选修/材料/验收
    │
    ▼
阶段 4：生成报告
    - 团队汇总报告（Word）
    - 单通详情作为附录或独立文件
    │
    ▼
汇报：返回核心摘要 + 管理动作清单
```

### 单通分析流程（子流程）

当用户明确只分析一通录音时，跳过"团队汇总"阶段，直接生成单通报告。
但输出格式仍需包含"管理洞察"维度（辅导优先级、一对一话术等）。

---

## 阶段 0：接收录音

### 0.1 定位录音文件

**批量场景：**
```bash
# 扫描指定目录下的所有录音
find "<目录路径>" -type f \( -name "*.m4a" -o -name "*.mp3" -o -name "*.wav" -o -name "*.amr" \) -mtime -7

# 按修改时间排序，统计数量
ls -lt "<目录路径>"/*.{m4a,mp3,wav,amr} 2>/dev/null | wc -l
```

**单通场景：**
用户直接给路径，或使用 find 搜索最近的录音。

**确认分析范围：**
批量时向用户确认：
> "找到 X 通录音，来自 Y 位销售，时长总计约 Z 分钟。预计分析时间约 W 分钟。是否继续？"

### 0.2 识别销售和客户信息

- 从文件名推断销售姓名（如 `小李_某客户_0520.m4a`）
- 从文件名推断客户类型
- 无法推断时标注"未知"

### 0.3 创建目标目录

```bash
mkdir -p ~/Desktop/录音洞察/团队/
mkdir -p ~/Desktop/录音洞察/<销售姓名>/
```

---

## 阶段 1：音频转写

### 前置依赖检查

```bash
which ffmpeg > /dev/null 2>&1 && echo "✅ ffmpeg" || echo "❌ brew install ffmpeg"
WHISPER=$(which whisper 2>/dev/null || find ~/Library/Python -name "whisper" -type f 2>/dev/null | head -1)
[ -n "$WHISPER" ] && echo "✅ whisper: $WHISPER" || echo "❌ pip3 install openai-whisper"
```

未安装则暂停并指导安装。

### 1.1 格式转换与转写

```bash
# 转单通
ffmpeg -i "<录音路径>" -ar 16000 -ac 1 "/tmp/recording.wav" -y
$WHISPER "/tmp/recording.wav" --model medium --language zh --output_dir /tmp --output_format txt --task transcribe

# 清理文本
python3 -c "
import re
with open('/tmp/recording.txt', 'r') as f: text = f.read()
text = re.sub(r'\n{3,}', '\n\n', text)
with open('/tmp/recording.txt', 'w') as f: f.write(text)
print(f'转写完成，{len(text)} 字符')
"
```

**批量时：** 循环处理每通录音，转写结果存为 `/tmp/recording_1.txt`, `/tmp/recording_2.txt`...

---

## 阶段 2：逐通分析（6大维度）

对每通录音，阅读转写文本，按以下 6 个维度分析。结果组织为 JSON。

### 维度一：通话摘要

提取核心信息：
1. 客户是谁（姓名/公司/职位）
2. 客户需求是什么
3. 客户异议是什么
4. 销售讲了什么核心内容
5. 结果是什么（具体到动作：约周三演示 / 发资料 / 再考虑）
6. 通话时长

输出：
```json
"call_summary": {
  "customer": "...", "need": "...", "objection": "...",
  "sales_pitched": "...", "outcome": "...", "duration": "X分X秒"
}
```

### 维度二：关键标签

给通话打三类标签：

**核心标签**（必选一个）：
- `高意向客户`——主动问价/方案/合同/付款
- `成交信号`——表达购买意愿或确认下一步
- `跟进机会`——未拒绝，需后续推动
- `无效沟通`——无实质内容或客户敷衍

**问题标签**（多选）：
`价格异议` `功能异议` `竞品对比` `决策人未触及` `销售失误`

**能力标签**（多选）：
`需求挖掘充分` `异议处理到位` `价值传递清晰` `推进动作明确`

输出：
```json
"tags": { "core": ["..."], "problems": ["..."], "strengths": ["..."] }
```

### 维度三：话术评分

从 5 个维度评分，满分 10 分，每个分数配一句话理由。

| 维度 | 考察点 |
|------|--------|
| 开场有效性 | 是否快速建立信任、说明来意、激发兴趣 |
| 需求挖掘充分度 | 是否问出真实需求、痛点、预算、决策流程 |
| 异议处理能力 | 面对价格/功能/竞品异议时处理是否得当 |
| 价值表达清晰度 | 产品价值是否说清楚，客户能否听懂 |
| 推进下一步明确度 | 结尾是否有明确的下一步动作（时间/地点/方式） |

**评分标准：** 9-10优秀/7-8良好/5-6及格/3-4较差/1-2很差

**综合得分：** 短板维度权重更高。若"推进下一步"仅 5 分，即使其他高，综合不超过 7 分。

输出：
```json
"scores": {
  "opening": {"score": 8, "reason": "..."},
  "need_discovery": {"score": 6, "reason": "..."},
  "overall_score": {"score": 6.8, "level": "良好但有明显短板"}
}
```

### 维度四：话术红黑榜

**红榜（亮点）：** 精彩的提问、成功的异议处理、精准的价值表达、有效的推进话术。每条标注"在哪个时刻"+"做了什么"。

**黑榜（硬伤）：** 每条包含：
- 问题描述（具体，不是"说得不好"）
- 示范话术（完整可替换的话术，不是"应该更委婉"）

常见硬伤：开场废话多、问句太浅、自说自话、异议处理对抗、价值模糊、结尾没动作、过度承诺、打断客户。

输出：
```json
"script_review": {
  "good_points": [{"moment": "...", "what": "..."}],
  "improvements": [{"moment": "...", "problem": "...", "suggestion": "..."}]
}
```

### 维度五：改进建议

输出三层可执行建议：

1. **立即改进**（下次就能用）：具体动作 + 示范话术
2. **短期提升**（本周练）：能力点 + 练习方式 + 验收标准
3. **长期成长**（持续优化）：方向 + 方法

输出：
```json
"actionable_advice": {
  "immediate": {"action": "...", "script": "..."},
  "short_term": {"focus": "...", "practice": "...", "success_criteria": "..."},
  "long_term": {"direction": "...", "method": "..."}
}
```

### 维度六：管理洞察

从管理者视角输出：
1. 核心问题（一句话）
2. 辅导优先级排序
3. 客户是否值得跟进 + 理由
4. 一对一辅导话术（给主管参考，明天说什么）
5. 团队经验（亮点可复制 / 问题需警惕）

输出：
```json
"management_insight": {
  "core_problem": "...",
  "coaching_priority": ["...", "..."],
  "customer_worth": {"judgment": "...", "reason": "..."},
  "one_on_one_script": "...",
  "team_lesson": {"positive": "...", "warning": "..."}
}
```

---

## 阶段 3：团队汇总诊断（批量分析必做）

当分析多通录音时，在所有逐通分析完成后，必须生成团队汇总。

### 3.1 横向对比

对比维度：
- 综合评分排名（谁最高/最低）
- 各维度评分对比（谁在开场强、谁在异议处理弱）
- 标签分布统计（多少通是高意向、多少通有价格异议）
- 进步/退步趋势（如有历史数据）

### 3.2 标杆萃取（>=8分录音）

从高分录音中提炼可复制的成功模式：

**成功模式**：高分销售的共同行为特征。用"X%的高分销售做了Y"格式。例如：
- "100%高分销售开场30秒内说明了来意"
- "80%的高分销售用具体案例讲清了产品价值"
- "75%的高分销售在客户异议后先反问再回应"

**可复制话术**：提取优秀话术原话，标注使用场景。

**最佳实践清单**：其他销售可以直接模仿的具体动作清单。

### 3.3 短板归因（<=4分或含严重失误录音）

从低分录音中提炼可归因的问题地图：

**问题分类地图**：按5个维度分类统计，用百分比展示。例如：
- 开场问题：60%（不会激发兴趣）
- 需求挖掘：40%（不问预算/决策流程）
- 异议处理：80%（一遇抗就掉节奏）
- 价值表达：50%（只会讲功能不会讲ROI）
- 推进下一步：90%（只说"保持联系"）

**高频失误TOP3**：本周出现最多的3类失误，标注出现频次。

**问题根因**：不是描述现象，而是归因到能力/认知/工具缺口。例如：
- 不是"开场说得不好"，而是"没有准备3套不同客户类型的开场话术"
- 不是"不会处理价格异议"，而是"没有帮客户算ROI的话术模板"

### 3.4 异议情报

统计所有录音中的客户异议：

**异议分布**：价格异议X%、功能异议Y%、竞品对比Z%、时机异议W%、信任异议V%...

**趋势变化**：本周 vs 上周异议类型占比变化。例如"价格异议从30%上升到50%"——说明客户对价格更敏感了。

**新出现的异议**：本周首次出现的新异议类型，需更新话术库。

**解决建议库**：针对本周高频异议（出现>=3次），每种给出3套不同风格的话术方案（直接回应型/反问引导型/案例对比型）。

### 3.5 产品表达审计

从客户对话中提取产品表达的真实反馈：

**客户理解偏差**：客户对产品价值的理解 vs 销售想传达的差距。例如：
- 销售想传达"自动化提效"，客户理解为"省了一个人工"
- 销售想传达"数据分析"，客户只关心"能不能导出Excel"

**价值传递盲点**：客户反复问的、但销售没主动讲清楚的功能/价值点。

**话术优化建议**：基于客户真实反馈，建议调整产品介绍的顺序、重点或话术。

### 3.6 SOP执行审计

检查实际录音中销售是否按标准流程执行：

**SOP执行率**：假设标准SOP有6步（开场→需求挖掘→产品介绍→异议处理→价值强化→推进下一步），统计每一步的执行率。

**SOP卡点**：哪个环节最容易被跳过或变形。例如：
- "需求挖掘"环节：80%的销售只问了一个需求问题就进入产品介绍
- "推进下一步"环节：90%的销售没有约定具体时间

**SOP优化建议**：基于实际录音，建议增加/删除/调整哪些步骤。例如：
- 增加"预算确认"步骤（发现70%的录音中客户最后才说预算不够）
- 删除"公司介绍"环节（发现客户普遍不耐烦，可缩短到10秒）

### 3.7 三层清单

**优秀录音清单（>=8分）**：
- 列出所有>=8分的录音：日期_姓名_客户类型_得分
- 标注核心亮点（一句话）
- 建议用途：内部分享/新人培训/话术库收录

**问题录音清单（<=4分或含严重失误）**：
- 列出所有<=4分或含"销售失误"标签的录音
- 标注核心问题和严重程度（🔴严重/🟡中等）
- 建议用途：一对一复盘/回炉培训/红线警告

**回炉名单（需要干预的人）**：
- 连续2周综合评分<5、或单通评分<3、或出现严重失误（过度承诺/辱骂客户/泄露机密等）
- 每个人的具体回炉计划：培训主题、培训时长、考核方式、考核期限
- 考核不通过进入淘汰预警

### 3.8 培训处方

基于以上所有分析，输出可直接执行的培训方案：

**本周必修**：
- 主题：（基于团队最大短板）
- 目标人群：（哪些人必须参加）
- 培训形式：（早会分享/中午集中训练/roleplay/录音复盘）
- 用时：（30分钟/1小时）
- 标杆案例：（用哪通录音做正面示范）
- 问题案例：（用哪通录音做反面教材）

**本周选修**：
- 针对特定人群的专项提升（如"开场弱的人选修开场训练营"）

**培训材料清单**：
- 需要准备的话术稿/案例数据/roleplay脚本/异议处理卡片

**验收标准**：
- 培训后通话评分应达到什么水平（如"下周综合评分>=7分的比例从30%提升到60%"）

---

## 阶段 4：生成报告

### 4.1 组织 JSON

将分析结果合并为 JSON，调用报告生成脚本：
```bash
python3 ~/.claude/skills/sales-recording-insight/scripts/generate_report.py /tmp/analysis_result.json
```

### 4.2 文件命名

- 单通：`日期_姓名_客户类型_录音洞察.docx`
- 批量汇总：`日期_团队_周汇总_录音洞察.docx`
- 日期格式：`年.月.日`（如 26.5.20）

---

## 汇报格式

### 批量分析汇报模板（给一线经理的"一页纸管理看板"）

```
✅ 团队分析完成！

📄 报告已保存：桌面/录音洞察/团队/日期_团队_周汇总_录音洞察.docx
单通详情：桌面/录音洞察/<姓名>/（各子文件夹）

📊 团队概览（共 X 通录音，Y 位销售）：
- 平均综合评分：X.X/10（上周 X.X，↑↓ X.X）
- 高意向：X通(X%) | 成交信号：X通(X%) | 需跟进：X通(X%) | 销售失误：X通(X%)

🏆 标杆萃取（>=8分共X通）：
成功模式：100%高分销售开场先确认预算；80%用案例讲清ROI
可复制话术：[场景：客户问价] "理解您的顾虑...方便问下您预算多少？"

🔍 短板归因（<=4分共X通）：
问题地图：开场60% | 需求挖掘40% | 异议处理80% | 价值表达50% | 推进下一步90%
高频失误TOP3：1.结尾没约时间(12次) 2.一问价就报底价(8次) 3.没问现有方案就推产品(7次)
根因：不是不会，是"没有准备3套开场话术模板"和"没有ROI计算表"

📢 异议情报（本周共X次异议）：
分布：价格45% | 功能20% | 竞品15% | 时机15% | 信任5%
趋势：价格异议↑15%——客户对价格更敏感了
新出现的异议："你们和XX平台有什么区别"（本周首次出现3次）

🎯 产品表达审计：
偏差：销售讲"自动化提效"，客户理解为"省了一个人工"
盲点：客户反复问"能不能导出Excel"，80%销售没主动讲
优化：开场后先问"您现在每个月花多少时间在数据整理上？"

📋 SOP执行审计：
执行率：开场85% | 需求挖掘35% | 产品介绍90% | 异议处理60% | 价值强化40% | 推进下一步10%
最大卡点：推进下一步（90%没执行）和需求挖掘（65%没深挖）
优化建议：增加"预算确认"步骤；把"公司介绍"从60秒压缩到10秒

📼 三层清单：
【优秀录音-用于培训】小王-某教育机构-8.5分 | 小李-某装修公司-8.0分
【问题录音-用于复盘】小张-某客户-3.5分🔴过度承诺 | 小陈-某客户-4.0分🟡开场废话
【回炉名单-需要干预】小张：连续2周<4分+过度承诺 → 培训2h+roleplay考核+1周复测>=6分

🎓 本周培训处方：
必修："如何优雅地推进下一步动作"
- 人群：全团队（90%有问题）| 形式：中午30分钟录音复盘+roleplay
- 标杆：小王-某教育机构 | 反面：小张-某客户
- 材料：3套推进话术模板 | 验收：下周执行率从10%→50%
选修："开场话术训练营"（针对开场<5分的人）

📋 明日管理动作：
- 早上：把高意向X通分配给对应销售，确认跟进状态
- 中午：30分钟培训——"如何推进下一步"，放小王标杆录音
- 晚上：一对一辅导小张练异议处理；警告小刘红线问题
```

### 单通分析汇报模板

```
✅ 录音分析完成！
📄 报告：桌面/录音洞察/XX/日期_XX_XX_录音洞察.docx

📊 核心摘要：
客户：XX | XX | X分X秒 | 意向：🟢高/🟡中/🔴低
综合评分：X.X/10 | 最强：...（X分）| 最弱：...（X分）
标签：...、...

💡 管理洞察：核心问题：... | 辅导优先级：...>...
客户跟进：... | 明天一对一：...

🎯 立即改进：...
```

---

## 参考文件

- **完整示例**：见 [references/examples.md](references/examples.md)
- **边界情况处理**：见 [references/edge-cases.md](references/edge-cases.md)
- **反模式与禁止事项**：见 [references/anti-patterns.md](references/anti-patterns.md)
